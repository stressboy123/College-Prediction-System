<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gdut.mapper.SysUserMapper">
    <select id="selectByUsername" resultType="com.gdut.entity.SysUser">
        SELECT id, username, password, nickname, status, create_time AS createTime
        FROM sys_user
        WHERE username = #{username}
    </select>
</mapper>